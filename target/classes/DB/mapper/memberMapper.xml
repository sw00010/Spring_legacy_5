<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.iu.s5.member.MemberDAO">
	
	<sql id="search">
		like '%'||#{search}||'%'
	</sql>
	
	<sql id="kind">
		<choose>
			<when test="kind=='mi'">
				id
			</when>
			<when test="kind=='mn'">
				name
			</when>
			<when test="kind=='me'">
				email
			</when>
			<otherwise>
				phone
			</otherwise>
		</choose>
	</sql>
	
	<select id="memberCount" resultType="java.lang.Long" parameterType="com.iu.s5.member.memberPage.MemberPager">
		select count(id) from member where 
		<include refid="kind"></include>
		<include refid="search"></include>
	</select>
	
	<select id="memberList" resultType="com.iu.s5.member.MemberVO" parameterType="com.iu.s5.member.memberPage.MemberPager">
		select * from member where 
		<include refid="kind"></include>
		<include refid="search"></include>
	</select>
	
	<select id="memberLogin" parameterType="com.iu.s5.member.MemberVO" resultType="com.iu.s5.member.MemberVO">
		select * from member where id=#{id} and pw=#{pw}
	</select>
	
	<insert id="memberJoin" parameterType="com.iu.s5.member.MemberVO">
		insert into member values(#{id}, #{pw}, #{name}, #{email}, #{phone}, #{age})
	</insert>
	
	<update id="memberUpdate" parameterType="com.iu.s5.member.MemberVO">
		update member set name=#{name}, email=#{email}, phone=#{phone}, age=#{age} where id=#{id}
	</update>
	
	<delete id="memberDelete" parameterType="com.iu.s5.member.MemberVO">
		delete member where id=#{id}
	</delete>


</mapper>  
